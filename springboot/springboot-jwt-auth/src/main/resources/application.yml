spring:
  data:
    mongodb:
      uri: ${MONGO_URI}
      auto-index-creation: true

logging:
  level:
    org:
      springframework:
        boot:
          context:
            config: DEBUG
#  security:
#    user:
#      name: user
#      password: user

server:
  port: 8081
  application:
    name: springboot-jwt-auth

jwt:
  secret: ${JWT_SECRET}
